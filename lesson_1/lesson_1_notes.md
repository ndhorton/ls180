# 1:6 The PostgreSQL Command Line Interface #

The meta-commands such as `\q` or `\h` are specific to the `psql` console. They
are not part of SQL, or even PostgreSQL in general, but specfically part of the
command line tool `psql` itself. They can only be used from within `psql`.


