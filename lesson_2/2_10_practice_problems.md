3.

The `Nullable` column for that column's row will contain `not null`.

5.

The constraint name, the type of constraint, and the data structure involved are
listed under `Indexes` below the table representing the table schema.

8.

`ERROR: new row for relations "films" violates check constraint`

9.

It is displayed under `Check constraints` rather than `Indexes`.

12.

Under `Check constraints`, the check is expressed using `>=` and `<=` rather
than `BETWEEN... AND` syntax.

14.

It appears under `Check constraints`.

15.

`ERROR:  new row for relation "films" violates check constraint "films_director_check"`

16.

Check constraints, data type, and `NOT NULL` constraint.

